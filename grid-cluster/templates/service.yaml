apiVersion: v1
kind: Service
metadata:
  name: {{ include "grid-cluster.fullname" . }}
  labels:
    {{- include "grid-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grid-cluster.selectorLabels" . | nindent 4 }}
