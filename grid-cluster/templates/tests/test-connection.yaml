apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "grid-cluster.fullname" . }}-test-connection"
  labels:
    {{- include "grid-cluster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "grid-cluster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
